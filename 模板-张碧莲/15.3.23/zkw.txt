void Change(int n,int v)
{
	for(T[n+=M]=(v==-1?0:1),n>>=1;n;n>>=1)
		T[n]=T[n+n]+T[n+n+1];
}

int Query(int s,int t)
{
	int ans=0;
	for(s=s+M-1,t=t+M+1;s^t^1;s>>=1,t>>=1)
	{
		if(~s&1) ans+=T[s^1];
		if(t&1) ans+=T[t^1];
	}
	return ans;
}
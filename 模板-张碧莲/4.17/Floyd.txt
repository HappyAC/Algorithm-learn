void Floyd() 
{//求网络中每对顶点间最短路径长度 
      int D[N][N],i,j,k;
      for (i=0; i<n; i++) 
            for (j=0; j<n; j++)
	       D[i][j]=mat[i][j]; 	// Initialize D(-1)[i, j]

      //递推求解每对顶点间最短路径长度
      for (k=0; k<n; k++) // n次试探,中间经过的顶点编号不大于k
	for (i=0;i<n; i++) // 遍历邻接矩阵
	       for (j=0;j<n; j++) // 遍历邻接矩阵
		   {          	
				if(D[i][j] > D[i][k]+D[k][j] && D[i][k]!=INT_MAX && D[k][j]!=INT_MAX)
			   {
				   D[i][j]=D[i][k]+D[k][j];	     
			   }
		   }
}